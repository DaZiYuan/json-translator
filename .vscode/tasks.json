{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "denon start",
            "type": "shell",
            "command": "denon start"
        },
        {
            "label": "test",
            "type": "shell",
            "command": "deno test --allow-env --allow-read --allow-net"
        }
    ]
}